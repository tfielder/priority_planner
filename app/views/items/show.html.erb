<p style="color: green"><%= notice %></p>

<%= render @item %>

<div>
  <%= link_to "Edit", edit_collection_item_path(@collection, @item) %> |
  <%= link_to "Back to items", collection_items_path(@collection) %> |
  <%= link_to "Delete", [@collection, @item], method: :delete %>
</div>
