<p style="color: green"><%= notice %></p>

<%= render @item %>

<div>
  <%= link_to "Edit this item", edit_collection_item_path(@collection, @item) %> |
  <%= link_to "Back to items", collection_items_path(@collection) %>

  <%= button_to "Destroy this item", [@collection, @item], method: :delete %>
</div>
